{
  "name": "page-pulse",
  "version": "1.0.0",
  "private": true,
  "description": "Page Pulse frontend and backend deployment workspace",
  "scripts": {
    "build": "npm run build --workspace frontend",
    "test": "npm test --workspace backend && npm test --workspace frontend",
    "start": "npm start --workspace backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": "^22.12.0"
  }
}
